pipeline {
  currentBuild.displayName = "fooName"
  agent any
  stages {
    stage('a') {
      steps {
        script{
        //def x=load 'packages/services/Jenkinsfile'
          print 'hi'
          //      x.start()
        }
      }
    }
   
  }
}
